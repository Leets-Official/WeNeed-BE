spring:
  datasource:
    url: jdbc:mysql://localhost:3306/weneed
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      #valicate로 바꾸기
      ddl-auto: create
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
jwt:
  issuer: jeongwan.noh@gmail.com
  secret_key: WeNeed